shape={}
shape.__index=self

-- Creates an instance
-- of the shape given a
-- provied set of props.
function shape:new()end

-- Approximates shape to a
-- convex polygon and returns
-- an array of its vertexes.
function shape:poly()end
